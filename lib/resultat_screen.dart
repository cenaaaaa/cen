import 'package:flutter/material.dart';

class ResultatScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Resulata'),),
    );
  }

}